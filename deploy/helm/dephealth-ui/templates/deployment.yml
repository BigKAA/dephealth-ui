apiVersion: apps/v1
kind: Deployment
metadata:
  name: dephealth-ui
  namespace: {{ include "dephealth-ui.namespace" . }}
  labels:
    {{- include "dephealth-ui.labels" . | nindent 4 }}
    app: dephealth-ui
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "dephealth-ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dephealth-ui.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yml") . | sha256sum }}
    spec:
      containers:
        - name: dephealth-ui
          image: {{ include "dephealth-ui.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "-config"
            - "/etc/dephealth-ui/config.yaml"
          {{- if .Values.customCA.enabled }}
          env:
            - name: SSL_CERT_FILE
              value: /etc/ssl/custom/{{ .Values.customCA.key }}
          {{- end }}
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: {{ .Values.probes.readinessDelay }}
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: {{ .Values.probes.livenessDelay }}
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/dephealth-ui
              readOnly: true
            {{- if .Values.customCA.enabled }}
            - name: custom-ca
              mountPath: /etc/ssl/custom
              readOnly: true
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: dephealth-ui-config
        {{- if .Values.customCA.enabled }}
        - name: custom-ca
          configMap:
            name: {{ .Values.customCA.configMapName }}
        {{- end }}
